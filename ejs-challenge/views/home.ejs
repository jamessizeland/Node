<%- include("partials/header") %> 

<h1>Home</h1>

<p><%= content %>  </p>

<% blogPosts.forEach(e => { %>
    <h2><%= e.title %></h2>
    <p><%= e.body.substring(0,100) %>...</p>
<% }) %>

<%- include("partials/footer") %>